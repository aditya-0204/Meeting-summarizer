{
  "buildCommand": "npm run build",
  "installCommand": "npm install --include=optional",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/health.ts": {
      "maxDuration": 60
    },
    "api/meetings/index.ts": {
      "maxDuration": 60
    },
    "api/meetings/[...path].ts": {
      "maxDuration": 60
    }
  }
}
